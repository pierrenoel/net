bool flag = true;

// string result = flag ? "You have access" : "You don't have access";
// Console.WriteLine(result);

Console.WriteLine($"{ (flag ? "You have access": "You don't have access") }");

